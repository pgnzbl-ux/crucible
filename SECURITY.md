# 安全策略

感谢你关注 Crucible 的安全。本文说明如何报告安全漏洞、处理时间线以及报告范围。

## 支持的版本

安全修复只发布在最新补丁版本中，请始终使用最新发布版。

| 版本    | 支持状态       |
| ------- | -------------- |
| v1.0.x（当前 v1.0.1） | ✅ 支持   |
| < v1.0.1 | ❌ 不支持，请先升级 |

## 报告漏洞

请通过 **GitHub 私密漏洞报告**提交：进入仓库的 `Security` 标签页，点击「Report a vulnerability」。

**请不要**通过公开 Issue、Pull Request 或 Discussions 报告安全漏洞，以免在修复发布前增加被利用的风险。

报告时建议包含：

- 受影响的版本（可通过 `GET /health` 返回的 `version` 字段确认）
- 复现步骤（尽可能给出最小化复现）
- 影响评估（例如：可泄露哪些凭据、能逃逸到什么程度、可越权访问哪些数据）
- 修复建议（可选）

## 处理流程与时间线

- **确认收到**：72 小时内回复
- **初步评估**：7 天内告知是否受理及严重程度
- **修复发布**：视严重程度而定，一般在 30 天内发布修复；严重问题优先处理
- **公开披露**：修复发布后，通过 GitHub Security Advisory 公开，并致谢报告者（如报告者同意）

## 报告范围

Crucible 是漏洞验证平台——**平台在目标仓库中分析出的漏洞是产品功能产出，不属于本安全策略的受理范围**。

### 纳入范围（Crucible 平台自身的漏洞）

- 认证 / 授权绕过：JWT 伪造、跨用户越权访问任务、报告或 Lab
- 凭据泄露：LLM API Key 或任务凭据经日志、API 响应或容器环境外泄
- Agent 沙箱逃逸：agent-runner 容器突破隔离，触及宿主机或平台内部服务
- Lab 隔离绕过：绕过 compose 策略获得 privileged、host 挂载、`docker.sock` 等危险能力
- LLM Provider SSRF 防护绕过

### 不纳入范围

- **Crucible 在目标仓库中分析出的漏洞**（见上文说明）
- 第三方依赖自身的漏洞：请直接报告给上游项目；可同步告知我们，以便评估对平台的影响
- 使用默认凭据（`crucible_secret` / `minioadmin`）且暴露公网的**开发实例**（README 已明确警告不要这样做）
- 偏离文档部署导致的问题（如生产环境未设置 `AUTH_SECRET`、CORS 配置为 `*`）
- 拒绝服务（DoS）、暴力破解、社会工程、钓鱼

## 安全部署提醒

- 生产环境必须设置足够强的 `AUTH_SECRET`，并修改 PostgreSQL / Redis / MinIO 的默认口令
- 不要将开发实例直接暴露到公网
- 凭据与隔离机制的详细说明见 [README.md](README.md) 的「凭据与安全」章节
